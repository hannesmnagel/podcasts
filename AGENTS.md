# Podcasts / The Podcatcher

## Routing
- iOS changes → check if a TestFlight build is needed (see `ios/AGENTS.md`)
- Backend/worker changes → deploy = push current branch to origin; say explicitly when no iOS build is needed

## Product priorities
- Smart Speed is on ice; do not work on it unless Hannes explicitly asks.
- Current priority is transcript alignment for DAI/local downloads.
- Future idea: user-selected ad transcript segments / similar-part skipping — check in before starting.
